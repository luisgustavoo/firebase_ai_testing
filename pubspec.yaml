name: firebase_ai_testing
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter
  firebase_core: ^4.2.1
  firebase_ai: ^3.6.0
  camera: ^0.11.3
  get_it: ^9.1.0
  injectable: ^2.6.0
  firebase_app_check: ^0.4.1+2
  go_router: ^17.0.0
  freezed_annotation: ^3.1.0
  http: ^1.2.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
  injectable_generator: ^2.9.1
  build_runner: ^2.10.4
  freezed: ^3.2.3
  json_serializable: ^6.11.2

flutter:
  uses-material-design: true
